import Select from './src/select.vue'
import Option from './src/option.vue'
import { installUtil } from '@st-ui/utils/install'
import './style'

export const StSelect = installUtil(Select)
export const StOption = installUtil(Option)
